using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class global : MonoBehaviour
{
    // Start is called before the first frame update
    public static bool staticChecker = false;
    public static int partChecker = 0, rowChecker = 0;

   /*  static void check_rot0 (GameObject objName, string findName){
        objName = GameObject.Find(findName);
        if (objName.transform.rotation == target0) {
        partChecker = 1;
        }
    }

    static void check_rot90 (GameObject objName, string findName){
        objName = GameObject.Find(findName);
        if (objName.transform.rotation == target90) {
        partChecker = 1;
        }
    }
    static void check_rot180 (GameObject objName, string findName){
        objName = GameObject.Find(findName);
        if (objName.transform.rotation == target180) {
        partChecker = 1;
        }
    }
    static void check_rot270 (GameObject objName, string findName){
        objName = GameObject.Find(findName);
        if (objName.transform.rotation == target270) {
        partChecker = 1;
        }
    }

    static void all_rot_check () {
        check_rot90(obj1, "gold (4)");
        check_rot90(obj2, "gold (4)");
        check_rot90(obj3, "gold (4)");
        check_rot90(obj4, "gold (4)");
        check_rot90(obj5, "gold (4)");
        check_rot90(obj6, "gold (4)");
        check_rot90(obj7, "gold (4)");
        check_rot90(obj8, "gold (4)");
        check_rot90(obj9, "gold (4)");
        check_rot90(obj10, "gold (4)");
        check_rot90(obj11, "gold (4)");
        check_rot90(obj12, "gold (4)");
        check_rot90(obj13, "gold (4)");
        check_rot90(obj14, "gold (4)");
        check_rot90(obj15, "gold (4)");
        check_rot90(obj16, "gold (4)");
    }


    public static GameObject obj1, obj2, obj3, obj4, obj5, obj6, obj7, obj8, obj9, obj10, obj11, obj12, obj13, obj14, obj15, obj16;
    public static GameObject row1, row2, row3, row4;
    Quaternion target0 = Quaternion.Euler(0, 0, 0);
    Quaternion target90 = Quaternion.Euler(0, 0, -90);
    Quaternion target180 = Quaternion.Euler(0, 0, 180);
    Quaternion target270 = Quaternion.Euler(0, 0, 90);
    private int partChecker = 0, rowChecker = 0;
 */
}
