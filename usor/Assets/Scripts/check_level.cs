using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.UI;
using UnityEngine.SceneManagement;

public class check_level : MonoBehaviour
{    
    /* void check_rot0 (GameObject objName, string findName, Quaternion target0, int partChecker){
        objName = GameObject.Find(findName);
        if (objName.transform.rotation == target0) {
        partChecker = 1;
        }
    }

    void check_rot90 (GameObject objName, string findName, Quaternion target90, int partChecker){
        objName = GameObject.Find(findName);
        if (objName.transform.rotation == target90) {
        partChecker = 1;
        }
    }
    void check_rot180 (GameObject objName, string findName, Quaternion target180, int partChecker){
        objName = GameObject.Find(findName);
        if (objName.transform.rotation == target180) {
        partChecker = 1;
        }
    }
    void check_rot270 (GameObject objName, string findName, Quaternion target270, int partChecker){
        objName = GameObject.Find(findName);
        if (objName.transform.rotation == target270) {
        partChecker = 1;
        }
    }

     void all_rot_check () {
        check_rot90(obj1, "gold (4)", target0, partChecker);
        check_rot90(obj2, "gold (4)", target0, partChecker);
        check_rot90(obj3, "gold (4)", target0, partChecker);
        check_rot90(obj4, "gold (4)", target0, partChecker);
        check_rot90(obj5, "gold (4)", target0, partChecker);
        check_rot90(obj6, "gold (4)", target0, partChecker);
        check_rot90(obj7, "gold (4)", target0, partChecker);
        check_rot90(obj8, "gold (4)", target0, partChecker);
        check_rot90(obj9, "gold (4)", target0, partChecker);
        check_rot90(obj10, "gold (4)", target0, partChecker);
        check_rot90(obj11, "gold (4)", target0, partChecker);
        check_rot90(obj12, "gold (4)", target0, partChecker);
        check_rot90(obj13, "gold (4)", target0, partChecker);
        check_rot90(obj14, "gold (4)", target0, partChecker);
        check_rot90(obj15, "gold (4)", target0, partChecker);
        check_rot90(obj16, "gold (4)", target0, partChecker);
    }

    public GameObject obj1, obj2, obj3, obj4, obj5, obj6, obj7, obj8, obj9, obj10, obj11, obj12, obj13, obj14, obj15, obj16;
    public GameObject row1, row2, row3, row4; 
    Quaternion target0 = Quaternion.Euler(0, 0, 0);
    Quaternion target90 = Quaternion.Euler(0, 0, -90);
    Quaternion target180 = Quaternion.Euler(0, 0, 180);
    Quaternion target270 = Quaternion.Euler(0, 0, 90);
    private int partChecker = 0, rowChecker = 0; */
    //public static int staticChecker = 0;

    
    //void Update()
    //{   
        //all_rot_check ();
        //Debug.Log (partChecker);
        /*row1 = GameObject.Find("circle-handle1");
        row2 = GameObject.Find("circle-handle2");
        row3 = GameObject.Find("circle-handle3");
        row4 = GameObject.Find("circle-handle4");

        if (row3.transform.position.y < row4.transform.position.y) {
            if (row3.transform.position.y < row2.transform.position.y){
                if (row1.transform.position.y > row2.transform.position.y) {
                    if (row1.transform.position.y > row2.transform.position.y){
                        if (row4.transform.position.y < row2.transform.position.y){
                            if (row4.transform.position.y < row1.transform.position.y){
                                global.rowChecker = 1;
                            }
                        }
                    }
                    
                    // Debug.Log(row1);
                }
                
            }
           //rowChecker = 1;
        }
       // if (partChecker==1 && rowChecker==1){
        //    global.staticChecker = true;
            //Debug.Log (global.staticChecker);
            //SceneManager.LoadScene("congrats", LoadSceneMode.Additive);
       // } 
    }
    //void change_scene () {
       // if (global.staticChecker == true){
        //    Debug.Log("errere");
        //    SceneManager.LoadScene("congrats");
      //  }
   // }*/
}
